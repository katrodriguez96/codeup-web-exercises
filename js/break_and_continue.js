let badNumber = prompt("Please enter an odd number anywhere from 1 to 50.");
for (let i = 1; i <=50; i+=2) {
    if (badNumber % 2 !== 0) {
        console.log("Number to skip: " + badNumber);
    } else {
        let badNumber = prompt("Incorrect. Please try t")
    }
}
